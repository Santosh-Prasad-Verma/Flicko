import { view } from './storybook.requires';

const StorybookUIRoot = view.getStorybookUI({
  // Uncomment to set initial selection:
  // initialSelection: { kind: 'TypingIndicator', name: 'Default' },
});

export default StorybookUIRoot;
