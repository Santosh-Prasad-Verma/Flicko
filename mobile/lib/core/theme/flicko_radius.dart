import 'package:mobile/core/constants/flicko_colors.dart' as tokens;

class FlickoRadius {
  FlickoRadius._();

  static const double sm = tokens.FlickoRadius.sm;
  static const double md = tokens.FlickoRadius.md;
  static const double lg = tokens.FlickoRadius.lg;
  static const double xl = tokens.FlickoRadius.xl;
  static const double round = tokens.FlickoRadius.round;
}
