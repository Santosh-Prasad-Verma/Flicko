import 'package:flutter/material.dart';
import 'package:mobile/core/constants/flicko_colors.dart' as tokens;

class FlickoColors {
  FlickoColors._();

  static const Color blurple = Color(tokens.FlickoColors.blurple);
  static const Color blurpleLight = Color(tokens.FlickoColors.blurpleLight);
  static const Color accentPrimary = Color(tokens.FlickoColors.accentPrimary);
  static const Color accentSecondary = Color(tokens.FlickoColors.accentSecondary);
  static const Color pink = Color(tokens.FlickoColors.pink);
  static const Color green = Color(tokens.FlickoColors.green);
  static const Color yellow = Color(tokens.FlickoColors.yellow);
  static const Color red = Color(tokens.FlickoColors.red);
  static const Color fuchsia = Color(tokens.FlickoColors.fuchsia);

  static const Color bgPrimary = Color(tokens.FlickoColors.bgPrimary);
  static const Color bgSecondary = Color(tokens.FlickoColors.bgSecondary);
  static const Color bgTertiary = Color(tokens.FlickoColors.bgTertiary);
  static const Color bgFloating = Color(tokens.FlickoColors.bgFloating);
  static const Color border = Color(tokens.FlickoColors.border);

  static const Color textPrimary = Color(tokens.FlickoColors.textPrimary);
  static const Color textSecondary = Color(tokens.FlickoColors.textSecondary);
  static const Color textMuted = Color(tokens.FlickoColors.textMuted);
  static const Color textDanger = Color(tokens.FlickoColors.textDanger);

  static const Color statusOnline = Color(tokens.FlickoColors.statusOnline);
  static const Color statusIdle = Color(tokens.FlickoColors.statusIdle);
  static const Color statusDnd = Color(tokens.FlickoColors.statusDnd);
  static const Color statusOffline = Color(tokens.FlickoColors.statusOffline);

  static const Color success = Color(tokens.FlickoColors.success);
  static const Color warning = Color(tokens.FlickoColors.warning);
  static const Color danger = Color(tokens.FlickoColors.danger);
}
