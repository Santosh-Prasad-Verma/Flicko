version: "3.9"

# Default local stack entrypoint.
# For full infrastructure use:
#   docker compose -f docker-compose.dev.yml up -d

services:
  redis:
    image: redis:7-alpine
    container_name: flicko-redis-local
    restart: unless-stopped
    ports:
      - "6379:6379"
